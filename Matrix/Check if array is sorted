class Solution {
    public boolean arraySortedOrNot(List<Integer> arr) {
        // code here
        for(int i=1;i<arr.size();i++){
            if(arr.get(i)<arr.get(i-1))
                return false;
        }
        return true;
        
    }
}

